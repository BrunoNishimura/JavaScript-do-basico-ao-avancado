//Aula 130:Loops e Arrays

/*
- Possibilidade de adicionar um conjunto de valores a uma variável;
- O Array deve ser escrito entre conchetes, separando os valores por vírgulas, veja:
*/

let numeros = [1, 3, 5, 7, 13];
let informacoes = ["Matheus", 12, true, "Teste", 2];

//Exemplo 1:
let arrNumeros = [1, 2, 3, 4, 5];
let arrNomes = ['Bruno', 'Daniel', 'Sassa'];
let arrBoolean = [true, false];
let arrMisturado = [1, "Bruno", true];

//Aula 131: Mais sobre Arrays
/*
- Podemos acessar um array pelo seu índice, por exemplo: arr[1];
- Lembrando que o primeiro índice sempre é o 0, ou seja, para acessar o primeiro elemento: arr[0]
*/

//Exemplo 1: Acesse o primeiro elemento do array arrNumeros
console.log(arr[0]);

//Exemplo 2: Acesse o último elemento do array arrNumeros
console.log(arr[4]);
//ou
console.log(arr.length -1);